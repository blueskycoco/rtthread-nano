source [find board/st_nucleo_f4.cfg]

init

proc flash_image {bin} {
    reset halt
    flash write_image erase gcc/$bin 0x08000000

    reset init
    reset
    shutdown
}

proc rst {} {
	reset
	shutdown
}
