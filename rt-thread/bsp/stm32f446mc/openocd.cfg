source [find board/st_nucleo_f4.cfg]

init

proc flash_image {} {
    reset halt
    flash write_image erase gcc/rtthread.bin 0x08000000

    reset init
    reset
    shutdown
}

proc rst {} {
	reset
	shutdown
}
