source [find interface/stlink.cfg]

transport select dapdirect_swd

source [find target/stm32f0x.cfg]

reset_config none
init

proc flash_image {} {
    reset halt
    flash write_image erase gcc/rtthread.bin 0x08000000

    reset init
    reset
    shutdown
}

proc rst {} {
	reset
	shutdown
}
